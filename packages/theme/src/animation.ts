/** Animation timing constants (ms). */
export const STAGGER_INTERVAL = 125;
export const FADE_DURATION = 400;
export const SPINNER_FADE_MS = 500;
export const QUICK_FADE_MS = 150;
export const FAST_FADE_MS = 200;
export const DEBOUNCE_MS = 250;
export const RESIZE_DEBOUNCE_MS = 150;
export const SETTINGS_RESTAGGER_DELAY = 500;
export const TRANSITION_MS = 300;
export const MIN_SPINNER_MS = 400;
export const STAGGER_ENTRY_OFFSET = 80;
export const ACCORDION_DELAY_MS = 300;
export const CHART_ANIM_DURATION = 400;
export const CHART_ANIM_SETTLE = 600;
export const DEMO_SWAP_INTERVAL_MS = 5_000;
export const SWIPE_THRESHOLD = 50;

/** Modal/popup scale-down factor for enter/exit animations. */
export const MODAL_SCALE_ENTER = 0.95;
/** Pill/button scale-down factor for hide animations. */
export const PILL_SCALE_HIDDEN = 0.9;
/** Modal slide-up offset (px) for enter animations. */
export const MODAL_SLIDE_OFFSET = 10;
/** Easing curve for layout transitions (position, size, border-radius). */
export const EASE_SMOOTH = 'cubic-bezier(0.4, 0, 0.2, 1)';
/** Overshoot easing for popup/menu scale-in. */
export const EASE_OVERSHOOT = 'cubic-bezier(0.34, 1.56, 0.64, 1)';
/** Stagger delay between row animations (ms). */
export const STAGGER_ROW_MS = 60;
/** FAB menu dismiss timeout (ms). */
export const FAB_DISMISS_MS = 300;
/** FAB menu open animation (ms). */
export const FAB_OPEN_MS = 450;
/** Short transition for hover/focus states (color, background). */
export const NAV_TRANSITION_MS = 150;
/** Medium transition for sidebar link state changes. */
export const LINK_TRANSITION_MS = 250;
/** Stagger interval for modal enter/exit animations (ms). */
export const MODAL_STAGGER_MS = 150;
