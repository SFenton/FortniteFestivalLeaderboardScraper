{
    "servers": {
        "playwright": {
            "command": "npx",
            "args": ["@playwright/mcp@latest", "--browser", "msedge", "--caps", "devtools,vision,testing,config"]
        },
        "fst-production": {
            "command": "node",
            "args": ["tools/mcp/fst-production-server.mjs"],
            "env": {
                "FST_BASE_URL": "${env:FST_BASE_URL}",
                "FST_API_KEY": "${env:FST_API_KEY}"
            }
        },
        "web-state": {
            "command": "node",
            "args": ["tools/mcp/fst-web-state-server.mjs"]
        }
    }
}
