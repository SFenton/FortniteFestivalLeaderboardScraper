<ContentPage
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:controls="clr-namespace:FortniteFestival.LeaderboardScraper.MAUI.Controls"
  xmlns:vm="clr-namespace:FortniteFestival.LeaderboardScraper.MAUI.ViewModels"
  xmlns:conv="clr-namespace:FortniteFestival.LeaderboardScraper.MAUI.Converters"
  xmlns:shapes="clr-namespace:Microsoft.Maui.Controls.Shapes;assembly=Microsoft.Maui.Controls"
  x:Class="FortniteFestival.LeaderboardScraper.MAUI.Pages.SongInfoPage"
  x:Name="SongInfoPageRoot"
  x:DataType="vm:SongInfoViewModel"
  Shell.NavBarIsVisible="false">
  <ContentPage.Resources>
    <conv:InverseBoolConverter x:Key="InverseBoolConverter" />
  </ContentPage.Resources>
  <!-- Layered background: album art dimmed -->
  <Grid>
    <Image Source="{Binding AlbumArtPath}" Aspect="AspectFill" />
    <BoxView Color="#88000000" />
    <!-- Foreground content wrapped in ScrollView for vertical overflow (excluding floating Back button) -->
    <ScrollView>
      <!-- Content grid; top padding adjusted at runtime to just clear floating Back button -->
  <!-- Top padding set dynamically in code-behind based on BackButton height to keep spacing visually balanced -->
  <Grid x:Name="ContentGrid" Padding="16,48,16,8" RowSpacing="14" RowDefinitions="Auto,Auto">
        <!-- Wide header layout (side-by-side) -->
  <Grid Grid.Row="0" ColumnDefinitions="Auto,Auto" ColumnSpacing="32" HeightRequest="260" IsVisible="{Binding UseCompactLayout, Converter={StaticResource InverseBoolConverter}}" HorizontalOptions="Center">
          <Grid Grid.Column="0" WidthRequest="220" HeightRequest="220" HorizontalOptions="Center" VerticalOptions="Center">
            <Border StrokeShape="RoundRectangle 18" BackgroundColor="#7A2B95" WidthRequest="220" HeightRequest="220" HorizontalOptions="Center" VerticalOptions="Center">
              <Image Source="{Binding AlbumArtPath}" Aspect="AspectFit" />
            </Border>
            <!-- Loading spinner overlay for album art -->
            <Border StrokeShape="RoundRectangle 18" BackgroundColor="#AA000000" WidthRequest="220" HeightRequest="220" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" VerticalOptions="Center">
              <ActivityIndicator IsRunning="{Binding IsLoading}" WidthRequest="60" HeightRequest="60" Color="#FFD700" HorizontalOptions="Center" VerticalOptions="Center" />
            </Border>
          </Grid>
          <VerticalStackLayout Grid.Column="1" Spacing="12" HorizontalOptions="Center" VerticalOptions="Center" WidthRequest="320">
            <Label Text="{Binding Title}" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="28" HorizontalTextAlignment="Center" LineBreakMode="WordWrap" />
            <Label Text="{Binding ArtistYearDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Center" LineBreakMode="WordWrap" />
          </VerticalStackLayout>
        </Grid>
        <!-- Compact header layout (stacked) -->
        <VerticalStackLayout Grid.Row="0" Spacing="16" HorizontalOptions="Center" VerticalOptions="Start" IsVisible="{Binding UseCompactLayout}" Padding="0,16">
          <Grid WidthRequest="220" HeightRequest="220" HorizontalOptions="Center" VerticalOptions="Center">
            <Border StrokeShape="RoundRectangle 18" BackgroundColor="#7A2B95" WidthRequest="220" HeightRequest="220" HorizontalOptions="Center" VerticalOptions="Center">
              <Image Source="{Binding AlbumArtPath}" Aspect="AspectFit" />
            </Border>
            <!-- Loading spinner overlay for album art (compact) -->
            <Border StrokeShape="RoundRectangle 18" BackgroundColor="#AA000000" WidthRequest="220" HeightRequest="220" IsVisible="{Binding IsLoading}" HorizontalOptions="Center" VerticalOptions="Center">
              <ActivityIndicator IsRunning="{Binding IsLoading}" WidthRequest="60" HeightRequest="60" Color="#FFD700" HorizontalOptions="Center" VerticalOptions="Center" />
            </Border>
          </Grid>
          <VerticalStackLayout Spacing="8" HorizontalOptions="Center" WidthRequest="320">
            <Label Text="{Binding Title}" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="28" HorizontalTextAlignment="Center" LineBreakMode="WordWrap" />
            <Label Text="{Binding ArtistYearDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="18" HorizontalTextAlignment="Center" LineBreakMode="WordWrap" />
          </VerticalStackLayout>
        </VerticalStackLayout>
        <!-- Instruments container sized to content with 90% opacity -->
        <Border Grid.Row="1" StrokeShape="RoundRectangle 26" BackgroundColor="#E6B35CD6" Padding="12" HorizontalOptions="Center" VerticalOptions="Start" Opacity="0.9">
          <Border.Triggers>
            <!-- In compact layout allow it to fill width -->
            <DataTrigger TargetType="Border" Binding="{Binding UseCompactLayout}" Value="True">
              <Setter Property="HorizontalOptions" Value="Fill" />
              <Setter Property="WidthRequest" Value="-1" />
            </DataTrigger>
            <!-- Wide layout: let content size itself (remove hard 1420 cap to avoid excess blank space); optionally cap with MaxWidthRequest if needed -->
            <DataTrigger TargetType="Border" Binding="{Binding UseCompactLayout}" Value="False">
              <Setter Property="WidthRequest" Value="1352" />
            </DataTrigger>
          </Border.Triggers>
          <VerticalStackLayout Spacing="4">
            <!-- Column headers (wide layout only) -->
            <Grid IsVisible="{Binding UseCompactLayout, Converter={StaticResource InverseBoolConverter}}" ColumnDefinitions="0,160,140,260,110,100,80,120,110,110" Padding="0,0" ColumnSpacing="14">
              <!-- Instrument header aligned with name column (left side roughly aligns visually with icon) -->
              <Label Grid.Column="1" Text="Instrument" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Start" />
              <Label Grid.Column="2" Text="Diff" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="3" Text="Stars" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="4" Text="Score" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="5" Text="Percent Hit" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="6" Text="Season" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="7" Text="Percentile" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="8" Text="Rank" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
              <Label Grid.Column="9" Text="Entries" FontFamily="NotoSansBold" FontAttributes="Bold" HorizontalTextAlignment="Center" />
            </Grid>
            <CollectionView ItemsSource="{Binding InstrumentRows}" SelectionMode="None">
          <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="vm:SongInfoInstrumentRow">
              <Grid>
                <!-- Wide layout -->
                <Grid x:Name="WideLayout" IsVisible="{Binding UseCompactLayout, Converter={StaticResource InverseBoolConverter}}" Padding="10,12" ColumnDefinitions="50,100,140,260,110,100,80,120,110,110" ColumnSpacing="14">
                  <Border Grid.Column="0" StrokeShape="Ellipse" BackgroundColor="#4B0F63" WidthRequest="48" HeightRequest="48" HorizontalOptions="Center" VerticalOptions="Center">
                    <Image Source="{Binding Icon}" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Center" />
                  </Border>
                  <Label Grid.Column="1" Text="{Binding Name}" FontFamily="NotoSansBold" FontAttributes="Bold" VerticalOptions="Center" LineBreakMode="TailTruncation" MaxLines="1" HorizontalTextAlignment="Start" />
      <!-- Difficulty Bars (parallelogram custom control) -->
                  <HorizontalStackLayout Grid.Column="2" Spacing="1" HorizontalOptions="Center" VerticalOptions="Center" BindableLayout.ItemsSource="{Binding DifficultyBars}">
                    <BindableLayout.ItemTemplate>
                      <DataTemplate x:DataType="vm:DifficultyBarVisual">
                        <controls:DifficultyBar WidthRequest="16" HeightRequest="34" FillColor="{Binding FillColor}" />
                      </DataTemplate>
                    </BindableLayout.ItemTemplate>
                  </HorizontalStackLayout>
                  <Grid Grid.Column="3" WidthRequest="260" HorizontalOptions="Center" VerticalOptions="Center">
                    <HorizontalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center" BindableLayout.ItemsSource="{Binding Stars}" IsVisible="{Binding HasScore}">
                      <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:StarVisual">
                          <Border StrokeShape="Ellipse" WidthRequest="48" HeightRequest="48" StrokeThickness="2" Stroke="{Binding OutlineColor}" BackgroundColor="Transparent" Padding="4" VerticalOptions="Center" HorizontalOptions="Center">
                            <Image Source="{Binding Image}" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Center" />
                          </Border>
                        </DataTemplate>
                      </BindableLayout.ItemTemplate>
                    </HorizontalStackLayout>
                    <Border IsVisible="{Binding ShowNA}" BackgroundColor="#2B4F8C" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Center" VerticalOptions="Center"><Label Text="N/A" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Center" VerticalOptions="Center" /></Border>
                  </Grid>
                  <Border Grid.Column="4" IsVisible="{Binding ShowScore}" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding ScoreDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                  </Border>
                  <Border Grid.Column="5" IsVisible="{Binding ShowPercent}" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding PercentDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" TextColor="White">
                      <Label.Triggers>
                        <DataTrigger TargetType="Label" Binding="{Binding IsFullCombo}" Value="True">
                          <Setter Property="TextColor" Value="#FFD700" />
                        </DataTrigger>
                      </Label.Triggers>
                    </Label>
                    <Border.Triggers>
                      <DataTrigger TargetType="Border" Binding="{Binding IsFullCombo}" Value="True">
                        <Setter Property="BackgroundColor" Value="#332915" />
                        <Setter Property="Stroke" Value="#FFD700" />
                        <Setter Property="StrokeThickness" Value="2" />
                      </DataTrigger>
                    </Border.Triggers>
                  </Border>
                  <Border Grid.Column="6" IsVisible="{Binding ShowSeason}" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding SeasonDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                  </Border>
                  <Border Grid.Column="7" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding PercentileDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" TextColor="White">
                      <Label.Triggers>
                        <DataTrigger TargetType="Label" Binding="{Binding IsTop5Percentile}" Value="True">
                          <Setter Property="TextColor" Value="#FFD700" />
                        </DataTrigger>
                      </Label.Triggers>
                    </Label>
                    <Border.Triggers>
                      <DataTrigger TargetType="Border" Binding="{Binding IsTop5Percentile}" Value="True">
                        <Setter Property="BackgroundColor" Value="#332915" />
                        <Setter Property="Stroke" Value="#FFD700" />
                        <Setter Property="StrokeThickness" Value="2" />
                      </DataTrigger>
                    </Border.Triggers>
                  </Border>
                  <Border Grid.Column="8" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding RankDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                  </Border>
                  <Border Grid.Column="9" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill" VerticalOptions="Center">
                    <Label Text="{Binding TotalEntriesDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                  </Border>
                </Grid>
                <!-- Compact layout (fully centered rows) -->
                <Grid x:Name="CompactLayout" IsVisible="{Binding UseCompactLayout}" Padding="10,12" HorizontalOptions="Fill">
                  <VerticalStackLayout Spacing="8" HorizontalOptions="Fill" VerticalOptions="Center">
                    <!-- Icon + Name -->
                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Center" VerticalOptions="Center">
                      <Border StrokeShape="Ellipse" BackgroundColor="#4B0F63" WidthRequest="48" HeightRequest="48" HorizontalOptions="Center" VerticalOptions="Center">
                        <Image Source="{Binding Icon}" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center" VerticalOptions="Center" />
                      </Border>
                      <Label Text="{Binding Name}" FontFamily="NotoSansBold" FontAttributes="Bold" VerticalOptions="Center" LineBreakMode="TailTruncation" MaxLines="1" />
                    </HorizontalStackLayout>
        <!-- Difficulty Bars (parallelogram custom control) -->
                    <HorizontalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center" BindableLayout.ItemsSource="{Binding DifficultyBars}">
                      <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:DifficultyBarVisual">
                          <controls:DifficultyBar WidthRequest="20" HeightRequest="40" FillColor="{Binding FillColor}" />
                        </DataTemplate>
                      </BindableLayout.ItemTemplate>
                    </HorizontalStackLayout>
                    <!-- Stars or N/A (match wide mode styling) -->
                    <Grid HorizontalOptions="Center" VerticalOptions="Center">
                      <HorizontalStackLayout Spacing="4" HorizontalOptions="Center" VerticalOptions="Center" BindableLayout.ItemsSource="{Binding Stars}" IsVisible="{Binding HasScore}">
                        <BindableLayout.ItemTemplate>
                          <DataTemplate x:DataType="vm:StarVisual">
                            <Border StrokeShape="Ellipse" WidthRequest="40" HeightRequest="40" StrokeThickness="2" Stroke="{Binding OutlineColor}" BackgroundColor="Transparent" Padding="3" VerticalOptions="Center" HorizontalOptions="Center">
                              <Image Source="{Binding Image}" WidthRequest="32" HeightRequest="32" HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                          </DataTemplate>
                        </BindableLayout.ItemTemplate>
                      </HorizontalStackLayout>
                      <Border IsVisible="{Binding ShowNA}" BackgroundColor="#2B4F8C" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="N/A" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Center" VerticalOptions="Center" />
                      </Border>
                    </Grid>
                    <!-- Compact metrics: descriptors above each value -->
                    <VerticalStackLayout Spacing="12" HorizontalOptions="Fill">
                      <!-- Row 1: Score | Percent -->
                      <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,*" ColumnSpacing="12" HorizontalOptions="Fill">
                        <Label Grid.Row="0" Grid.Column="0" Text="Score" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="12" HorizontalTextAlignment="Center" Margin="0,0,0,2" />
                        <Label Grid.Row="0" Grid.Column="1" Text="Percent Hit" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="12" HorizontalTextAlignment="Center" Margin="0,0,0,2" />
                        <Border Grid.Row="1" Grid.Column="0" BackgroundColor="#1D3A71" Stroke="Transparent" StrokeShape="RoundRectangle 10" Padding="8,4" IsVisible="{Binding ShowScore}" HorizontalOptions="Fill" >
                          <Label Text="{Binding ScoreDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Center" HorizontalTextAlignment="Center" />
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" StrokeShape="RoundRectangle 10" Padding="8,4" IsVisible="{Binding ShowPercent}" BackgroundColor="#1D3A71" HorizontalOptions="Fill">
                          <Label Text="{Binding PercentDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Center" HorizontalTextAlignment="Center" TextColor="White">
                            <Label.Triggers>
                              <DataTrigger TargetType="Label" Binding="{Binding IsFullCombo}" Value="True">
                                <Setter Property="TextColor" Value="#FFD700" />
                              </DataTrigger>
                            </Label.Triggers>
                          </Label>
                          <Border.Triggers>
                            <DataTrigger TargetType="Border" Binding="{Binding IsFullCombo}" Value="True">
                              <Setter Property="BackgroundColor" Value="#332915" />
                              <Setter Property="Stroke" Value="#FFD700" />
                              <Setter Property="StrokeThickness" Value="2" />
                            </DataTrigger>
                          </Border.Triggers>
                        </Border>
                      </Grid>
                      <!-- Row 2: Season | Percentile -->
                      <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,*" ColumnSpacing="12" HorizontalOptions="Fill">
                        <Label Grid.Row="0" Grid.Column="0" Text="Season" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="12" HorizontalTextAlignment="Center" Margin="0,0,0,2" />
                        <Label Grid.Row="0" Grid.Column="1" Text="Percentile" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="12" HorizontalTextAlignment="Center" Margin="0,0,0,2" />
                        <Border Grid.Row="1" Grid.Column="0" IsVisible="{Binding ShowSeason}" BackgroundColor="#1D3A71" Stroke="Transparent" Padding="8,4" StrokeShape="RoundRectangle 10" HorizontalOptions="Fill">
                          <Label Text="{Binding SeasonDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Center" HorizontalTextAlignment="Center" />
                        </Border>
                        <Border Grid.Row="1" Grid.Column="1" BackgroundColor="#1D3A71" Stroke="Transparent" StrokeShape="RoundRectangle 10" Padding="8,4" HorizontalOptions="Fill">
                          <Label Text="{Binding PercentileDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" TextColor="White">
                            <Label.Triggers>
                              <DataTrigger TargetType="Label" Binding="{Binding IsTop5Percentile}" Value="True">
                                <Setter Property="TextColor" Value="#FFD700" />
                              </DataTrigger>
                            </Label.Triggers>
                          </Label>
                          <Border.Triggers>
                            <DataTrigger TargetType="Border" Binding="{Binding IsTop5Percentile}" Value="True">
                              <Setter Property="BackgroundColor" Value="#332915" />
                              <Setter Property="Stroke" Value="#FFD700" />
                              <Setter Property="StrokeThickness" Value="2" />
                            </DataTrigger>
                          </Border.Triggers>
                        </Border>
                      </Grid>
                      <!-- Row 3: Combined Rank -->
                      <VerticalStackLayout Spacing="2" HorizontalOptions="Fill">
                        <Label Text="Rank" FontFamily="NotoSansBold" FontAttributes="Bold" FontSize="12" HorizontalTextAlignment="Center" />
                        <Border BackgroundColor="#1D3A71" Stroke="Transparent" StrokeShape="RoundRectangle 10" Padding="8,4" HorizontalOptions="Fill">
                          <Label Text="{Binding RankOutOfDisplay}" FontFamily="NotoSansBold" FontAttributes="Bold,Italic" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                        </Border>
                      </VerticalStackLayout>
                    </VerticalStackLayout>
                  </VerticalStackLayout>
                </Grid>
              </Grid>
            </DataTemplate>
          </CollectionView.ItemTemplate>
          </CollectionView>
          </VerticalStackLayout>
        </Border>
      </Grid>
    </ScrollView>
    <!-- Floating Back button (stays fixed while scrolling) -->
  <Border StrokeShape="RoundRectangle 24" BackgroundColor="#4B0F63" Padding="16,10" x:Name="BackButton" HorizontalOptions="Start" VerticalOptions="Start" Margin="16,16,0,0" ZIndex="10">
      <Border.GestureRecognizers>
        <TapGestureRecognizer Tapped="OnBackTapped" />
      </Border.GestureRecognizers>
      <HorizontalStackLayout Spacing="6" VerticalOptions="Center">
        <Label Text="←" FontFamily="NotoSansBold" FontAttributes="Bold" TextColor="White" />
        <Label Text="Back" FontFamily="NotoSansBold" FontAttributes="Bold" TextColor="White" />
      </HorizontalStackLayout>
    </Border>
  </Grid>
</ContentPage>
